// スコアに応じた称号（レベル）。しきい値は RANK_STEP 刻みで、後から変えやすいよう集約。
export const RANK_STEP = 2000;             // 称号が上がるスコア間隔
// しきい値 = インデックス × RANK_STEP（0,2000,4000,…）。最後の称号で頭打ち。
export const RANKS = [
  "カビ取り見習い",        // 0
  "カビ取り初級",          // 2000
  "カビ取り中級",          // 4000
  "カビ取り上級",          // 6000
  "カビ取り職人",          // 8000
  "カビ取り熟練職人",      // 10000
  "カビ取りマイスター",    // 12000
  "カビ取り達人",          // 14000
  "カビ取り師範",          // 16000
  "カビ取り師範代",        // 18000
  "カビ取り隊長",          // 20000
  "カビ取り副長",          // 22000
  "カビ取り団長",          // 24000
  "カビ取り博士",          // 26000
  "カビ取り騎士",          // 28000
  "カビ取り近衛",          // 30000
  "カビ取り将軍",          // 32000
  "カビ取り大将",          // 34000
  "カビ取り賢者",          // 36000
  "カビ取り聖者",          // 38000
  "カビ取りマスター",      // 40000
  "カビ取りグランドマスター", // 42000
  "カビ取り皇帝",          // 44000
  "カビ取り大帝",          // 46000
  "カビ取り神",            // 48000
  "伝説のカビ取りキング",   // 50000以上
];

// スコア→称号インデックス（最大で最後の称号に丸める）
export function rankIndex(score) {
  return Math.min(RANKS.length - 1, Math.max(0, Math.floor(score / RANK_STEP)));
}
export function rankName(score) {
  return RANKS[rankIndex(score)];
}
export function rankNameByIndex(i) {
  return RANKS[Math.min(RANKS.length - 1, Math.max(0, i))];
}
